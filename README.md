# Araz Flow 2.0.0 — Build 012

نسخه اصلاحی ذخیره‌سازی و عیب‌یابی. خطای `updateBackupPanel` که صف ذخیره‌سازی و آزمایش نوشتن/خواندن را متوقف می‌کرد رفع شده است. اصلاحات کش و Service Worker از Build 011 حفظ شده‌اند. Schema داده همچنان 7 است.
